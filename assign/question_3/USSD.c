#include <stdio.h>
int main()
{
    /* code */
    char code = '*117#';
    int userCode;
    scanf("%d", &userCode);
    if (userCode == code){
        /* code */
    }else{
        printf("Invalide USSD code\n");
    }
    
    return 0;
}
