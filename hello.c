#include <stdio.h>


// to run this program you should first run gcc -o fileName fileName.c
// the you can now write the fileName you worte 
//  for example to run this file we can run the following commands 
// gcc -o hello hello.c
//  ./hello
int main(void)
{
   
   int x = 5;
   x /= 2;

   printf("%d\n", x);
}
